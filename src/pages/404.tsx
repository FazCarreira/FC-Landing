import { FunctionComponent } from 'react';

const NotFound: FunctionComponent = () => {
  return <img src='/images/404.jpg' className='my-5' alt='Pagina não encontrada' />;
};

export default NotFound;
